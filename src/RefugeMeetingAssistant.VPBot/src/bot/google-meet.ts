/**
 * Google Meet Handler (STUB - Not yet implemented)
 */

import { Page } from 'playwright';

export class GoogleMeetHandler {
  static async join(page: Page, botName: string): Promise<void> {
    throw new Error('Google Meet not yet supported. Only Teams is currently implemented.');
  }
}
