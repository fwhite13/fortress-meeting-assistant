/**
 * Zoom Meeting Handler (STUB - Not yet implemented)
 */

import { Page } from 'playwright';

export class ZoomHandler {
  static async join(page: Page, botName: string): Promise<void> {
    throw new Error('Zoom meetings not yet supported. Only Teams is currently implemented.');
  }
}
